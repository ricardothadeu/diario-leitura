<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Livro</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>Editar Livro</h1>
    <form id="editBookForm" action="/livros/<%= livro.id %>?_method=PUT" method="POST">
        <label for="titulo">Título:</label>
        <input type="text" id="titulo" name="titulo" value="<%= livro.titulo %>" required><br>

        <label for="autor">Autor:</label>
        <input type="text" id="autor" name="autor" value="<%= livro.autor %>" required><br>

        <label for="numeroPaginas">Número de Páginas:</label>
        <input type="number" id="numeroPaginas" name="numeroPaginas" value="<%= livro.numeroPaginas %>"><br>

        <label for="dataInicio">Data de Início:</label>
        <input type="date" id="dataInicio" name="dataInicio" value="<%= livro.dataInicio ? livro.dataInicio.toISOString().split('T')[0] : '' %>"><br>

        <label for="dataTermino">Data de Término:</label>
        <input type="date" id="dataTermino" name="dataTermino" value="<%= livro.dataTermino ? livro.dataTermino.toISOString().split('T')[0] : '' %>"><br>

        <label for="nota">Nota (0-5):</label>
        <input type="number" id="nota" name="nota" min="0" max="5" value="<%= livro.nota %>"><br>

        <label for="comentarios">Comentários:</label>
        <textarea id="comentarios" name="comentarios"><%= livro.comentarios %></textarea><br>

        <button type="submit">Salvar</button>
    </form>
    <button onclick="window.location.href='/livros/'">Voltar</button>
</body>
</html>
